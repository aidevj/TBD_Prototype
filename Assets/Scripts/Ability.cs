using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ability : MonoBehaviour {

    //placeholder for now, will fill out later, public for testing
    public string name;
    public int cost;
    public string type; //offense, heal, etc.
    public string element;
    public string effect; //may need to use a different variable for this
    public int damageMult;
    public int range;
    public string rangeType; //single or multiple targets

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
